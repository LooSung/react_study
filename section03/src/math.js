export function add(a, b) {
  return a + b;
}

export function sub(a, b) {
  return a - b;
}

// Common JS CJS
// module.exports = {
//   add: add,
//   sub: sub,
// };

// ES Module System
// export { add, sub };

export default function mutiply(a, b) {
  return a * b;
}
